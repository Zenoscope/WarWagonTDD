using System;

namespace Main{
  class Main {
  	public static void Main()
  	{
      // blah start menu, load things in
      
      // set the wagon type from previous settings
      // start the game (is it single session or does it save after each time) 
      // load in the wagon and drive around
      // spawn enemies - enemies die
      // numbers go up
      // if the player dies, restart or go back to the start menu
      // if the player quits, go back to the start menu
      
  	}
  }
}
